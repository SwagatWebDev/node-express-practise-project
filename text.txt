return groupedByNameAndPathAndDay.values().stream()
        .flatMap(events -> Stream.of(
                pickRecordWithMinAndMaxValues(events, EventLogProjection::getOldValue),
                pickRecordWithMinAndMaxValues(events, EventLogProjection::getNewValue)
        ))
        .map(this::mapToEventLogDto)
        .collect(Collectors.toList());
